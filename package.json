{
  "name": "pdf-splitter",
  "version": "0.1.0",
  "description": "A command-line utility for splitting PDF documents.",
  "main": "src/index.js",
  "bin": {
    "quickpdfsplit": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test test/**/*.test.js",
    "build:rust": "cd rust_splitter && cargo build --release && cd .. && mkdir -p bin && cp rust_splitter/target/release/librust_splitter.a bin/rust_splitter"
  },
  "keywords": [
    "pdf",
    "split",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {}
} 